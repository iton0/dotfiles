#!/usr/bin/env bash

# Make sure in the home directory
cd

# Update package list
sudo apt update

# Install Git, Vim, Tmux, Zsh, and other tools
sudo apt install -y vim tmux zsh fzf sed curl tar gzip ripgrep unzip wget zip xclip || {
    echo "Error: Installation of Vim, Tmux, Zsh, fzf, sed, curl, tar, gzip, ripgrep, unzip, wget, zip, xclip failed."
    exit 1
}

# Set Zsh as the default shell and start Zsh
chsh -s "$(which zsh)"
zsh

# Output success message
echo "Tools (Vim, Tmux, Zsh, fzf, sed, curl, tar, gzip, ripgrep, unzip, wget, zip, xclip) installed successfully."

# Upgrade packages
sudo apt upgrade -y
echo "--------------------------------"
echo ""

# Install lazygit
echo "Installing LazyGit"
LAZYGIT_VERSION=$(curl -s "https://api.github.com/repos/jesseduffield/lazygit/releases/latest" | grep -Po '"tag_name": "v\K[^"]*')
curl -Lo lazygit.tar.gz "https://github.com/jesseduffield/lazygit/releases/latest/download/lazygit_${LAZYGIT_VERSION}_Linux_x86_64.tar.gz"
tar xf lazygit.tar.gz lazygit
sudo install lazygit /usr/local/bin
echo "Current LazyGit version:"
lazygit --version
rm lazygit.tar.gz
echo "--------------------------------"
echo ""

# Prompt to continue for Neovim installation
echo "Installing Neovim"
curl -LO "https://github.com/neovim/neovim/releases/download/stable/nvim-linux64.tar.gz"
tar xzvf nvim-linux64.tar.gz -C /opt/
echo "--------------------------------"
echo "Current Neovim version:"
/opt/nvim-linux64/bin/nvim --version
rm nvim-linux64.tar.gz
